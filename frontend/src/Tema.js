import { createTheme } from "@mui/material/styles";
const Tema = createTheme({
  palette: {
    common: {
      black: "#ADD8E6",
      white: "#fff3e0",
      blackD: "#008000",
      whiteD: "#fff3e0",
      blackT: "#FFA500",
      whiteT: "#fff3e0",
    },
  },
});
export default Tema;
